#lang racket

;; first the values of a and b will be evaluated
;; then the if operator will evaluate to + if b is positive and to - if b is negative 
;; in any case the procedure will return the sum of a and the absolute value of b